# Cloudflare Pages configuration for local preview and CLI deploys.
# Git-connected Pages projects use dashboard build settings (see README).
name = "blue-ox-tree-care"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-01"
